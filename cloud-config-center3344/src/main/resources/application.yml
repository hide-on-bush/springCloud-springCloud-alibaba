server:
  port: 3344

spring:
  application:
    name: cloud-config-center
  cloud:
    config:
      server:
        git:
          uri: https://github.com/hide-on-bush/springcloud-config.git
          ###搜索目录
          search-paths:
            - springcloud-config
      ## 读取分支
      label: main

#注册到eureka
eureka:
  client:
    service-url:
      defaultZone: http://eureka7001.com:7001/eureka,http://eureka7002.com:7002/eureka